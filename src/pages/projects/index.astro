---
import Layout from "../../layouts/default.astro";
import IndexList from "../../components/PostList/PostGroups.astro";
const modules = import.meta.glob("./*.md", { eager: true });
const posts = Object.values(modules);
---

<Layout>
  <div class="feed">
    <div class="titleBox">
      <h1 class="title">my projects 💡︎</h1>
      <p class="about">Here i ... and a bit of sssdjkad. enjoy!</p>
    </div>
    <IndexList posts={posts} groups={["two"]} />
  </div>
</Layout>

<!-- ######################## CSS ######################## -->

<style>
  .feed {
    margin: auto;
    margin-top: 2em;
    margin-bottom: 6em;
    padding: 0em 1em;
    .titleBox {
      padding-bottom: 1em;
      .title {
        text-align: center;
        color: var(--color-primary);
        border-bottom: 1px solid var(--color-bg-dark);
        padding-bottom: 0.3em;

        text-shadow: 0.03em 0.03em 0.1em var(--color-bg-dark);
      }

      .about {
        margin-top: 1em;
        color: var(--color-text-blockquote);
      }
    }
  }
</style>
