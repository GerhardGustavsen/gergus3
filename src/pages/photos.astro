---
import Layout from "../layouts/default.astro";
import Gallery from "../components/Gallery/Gallery.astro";
const mods = import.meta.glob("../assets/photos/*.{jpg,jpeg,png,webp,avif}", {
    eager: true,
    import: "default",
});

const urls = Object.values(mods);

console.log(urls);
---

<Layout>
    <div class="feed">
        <div class="titleBox">
            <h1 class="title">my photoes 🖼</h1>
            <p class="about">Wait a sec...</p>
        </div>
    </div>
    <Gallery images={urls} maxSize={350} delayMs={100} rotationRange={20} />
</Layout>

<!-- ######################## CSS ######################## -->

<style>
    .feed {
        margin: auto;
        margin-top: 2em;
        margin-bottom: 6em;
        padding: 0em 1em;
        .titleBox {
            padding-bottom: 1em;
            .title {
                text-align: center;
                color: var(--color-primary);
                border-bottom: 1px solid var(--color-bg-dark);
                padding-bottom: 0.3em;

                text-shadow: 0.03em 0.03em 0.1em var(--color-bg-dark);
            }

            .about {
                margin-top: 1em;
                color: var(--color-text-blockquote);
            }
        }
    }
</style>
