---
interface Props {
    title: string;
    about?: string;
}

const { title, about } = Astro.props;
---

<div class="titleBox">
    <h1 class="title">{title}</h1>
    <p class="about">{about}</p>
</div>

<!-- ######################## CSS ######################## -->

<style>
    .titleBox {
        padding: 1em;
        padding-bottom: 6em;

        .title {
            text-align: center;
            color: var(--color-primary);
            border-bottom: 1px solid var(--color-bg-dark);
            padding-bottom: 0.3em;

            text-shadow: 0.03em 0.03em 0.1em var(--color-shadow);
        }

        .about {
            margin-top: 1em;
            color: var(--color-text-blockquote);
        }
    }
</style>
